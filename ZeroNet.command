#!/bin/bash

screen -S ZeroNet -d -m /Applications/ZeroBundle/ZeroNet.app;
echo "ZeroNet is now booting. This may take a while, but then the ZeroNet-homepage will launch in your main browser. This terminal can now be closed."
exit;
